{
  "name": "custom-new-tab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd edge && zip -r ../custom-new-tab-edge.zip . && cd ..",
    "build-firefox": "cd firefox && zip -r ../custom-new-tab-firefox.zip . && cd .."
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
